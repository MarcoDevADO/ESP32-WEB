[project]
name = "accelerometer-web-dashboard"
version = "0.1.0"
description = "Accelerometer data web dashboard from MATLAB"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "flask>=3.0.0",
    "flask-cors>=4.0.0",
    "reportlab>=4.0.0",
    "gunicorn>=25.1.0",
]